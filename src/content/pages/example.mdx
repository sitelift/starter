---
title: Example page
description: A sample content-collection page. Duplicate this pattern for new pages.
draft: false
---

# Example page

Content collections store pages as `.md` or `.mdx` files in `src/content/pages/`.

Each file needs frontmatter with at least a `title`. This one is served at `/example`.

You can use **Markdown** and, in `.mdx` files, Astro components when you need them.
